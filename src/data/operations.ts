export const revenueTrend: Array<{ period: string; sales: number; purchases: number; profit: number }> = [];

export const productMovement: Array<{ product: string; inward: number; outward: number }> = [];

export const topProducts: Array<{ name: string; units: number; revenue: number; growth: string }> = [];

export const topCategories: Array<{ category: string; value: number }> = [];

export const bestCustomers: Array<{ name: string; village: string; spend: number; orders: number }> = [];

export const salesHeatmap: Array<{ day: string; slots: number[] }> = [];
